const PARTITION_KEY_DEFINITION_VERSION = {
	v2: 2,
};

const PARTITION_KEY_KIND = {
	MultiHash: 'MultiHash',
};

const TTL_ON = 'On';
const TTL_OFF = 'Off';
const TTL_ON_DEFAULT = 'On (no default)';

module.exports = {
	PARTITION_KEY_DEFINITION_VERSION,
	PARTITION_KEY_KIND,
	TTL_ON,
	TTL_OFF,
	TTL_ON_DEFAULT,
};
